*получить изменения с сервера:* `git fetch`

*посмотреть ветки:* `git branch --all`

*обьединить ветку name с основной веткой:* `git merge name`

**Не забывай про `git status`, `git add`, `git commit -m` и `git push`!**